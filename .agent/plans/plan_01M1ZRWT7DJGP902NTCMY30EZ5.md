# Repair all missing card display while preserving 0.5.2 financial replay

Implement the approved recommendation for syrup-rail-g75 on integration/0.5.3 at cd5970b. Existing financial sale/query descriptor semantics must match v0.5.2. An additive, explicitly metadata-only query supplies card brand, last four digits, expiry month and expiry year to the existing fill-only refresh. The original all-blank display case is part of acceptance; expiry only exposed the replay blocker. Do not change immutable financial equality, migrations, or historical charges, and exclude fixes already in master.

## Progress
- [x] Revalidated clean application source, current master 6fccde0, caller paths, crate guides and threat model.
- [x] Add distinct metadata observation contracts and a default gateway query capability.
- [x] Keep NMI financial normalization compatible; share exact-query validation/transport with the enriched metadata path.
- [x] Route saved-method refresh to metadata query, preserving existing fences and cooldowns.
- [x] Prove old parked approvals reconcile via actual NMI parsing and later acquire expiry without financial evidence changes; prove actual NMI queries repair the original all-blank case too.
- [x] Update API/host guidance and run relevant checks and comprehensive review before commit, up to three rounds.
- [x] Commit; review the full branch against v0.5.2 (two rounds completed), address compatible findings, and identify breaking follow-ups for Beads.
- [x] Finish fresh required gates, close g75, and record the deferred gateway contract follow-up. Completion record prepared for commit.

## Surprises & Discoveries
Expiry and card-brand descriptor fields are part of strict immutable charge replay equality. NMI Classic and XML enrichment changed those fields for previously retained responses. JSON already parsed expiry in 0.5.2 and must retain that behavior. The new gateway query capability is absent from master. PostgreSQL schema v4 remains the release contract.

## Decision Log
Use a distinct metadata result with identity, status/diagnostics for rejection, and safe display fields, with no conversion to financial approval authority. Default provider implementation can project an existing query result; NMI overrides it to enrich only metadata queries. Preserve existing public constructors/parts shapes and do not add a required trait method. Raw NMI client and core contracts remain provider/SQL independent. Read docs/security/threat-model.md; this change creates no new authentication, retention or financial authority boundary.

## Outcomes & Retrospective
Implemented in 12c36d7, with host guidance clarified in 0206cb3. Brand, last four, expiry month and expiry year are repaired through the distinct metadata query, including actual-NMI/PostgreSQL coverage of the original all-blank case. Financial Classic/XML normalization matches released v0.5.2; existing JSON expiry and strict immutable replay equality are preserved. No schema, version, runtime dependency or third-party dependency version changed. The only manifest/lockfile delta is test support: a PostgreSQL dev-dependency on the NMI adapter, an explicit Tokio io-util dev feature, and the corresponding internal lockfile edge. This supersedes the manifest/dependency statements in earlier completion snapshots.

Three focused and two full-branch Claude/Codex review rounds completed, with complete stable fingerprints and restricted Claude access; both final branch passes had reviewer-attested 28/28-page coverage. Findings were verified against v0.5.2, master and the documented host/security boundaries. Compatible documentation/test gaps were addressed; breaking financial normalization and gateway-contract redesign remain deferred. Final fresh gates passed on 0206cb3: fmt, Clippy, contract, SQLx, 633 tests and six doctests (269 PostgreSQL tests in 623.51 seconds). Public API checks, Rust 1.88 all-target workspace compilation, standalone NMI-client/MSRV checks, schema immutability and repository guide/LOC checks passed. The first full suite also passed; a documentation-only follow-up invalidated its work fingerprint, so the required gates were repeated on the settled revision.

No live/staging provider response capture, host rollout, version preparation, publication or tag occurred. Host invocation/decorator validation remains separate under syrup-rail-20u. Existing Beads export includes unrelated preexisting changes and remains unstaged; only the reviewed application/guidance and task-owned workflow records are committed.

The Jig plan/session closed successfully after all fresh required gates passed. Closed syrup-rail-g75 with the upgrade and all-blank regression evidence. Financial normalization evolution remains in syrup-rail-x19; the potentially breaking gateway forwarding/capability contract is now syrup-rail-cis, both deferred to 0.6.0. Added metadata-backfill traffic to the existing load-rehearsal follow-up syrup-rail-bn4. Kept syrup-rail-20u open for host staging/invocation and release acceptance and recorded the library completion there. The Beads export was refreshed without staging its unrelated preexisting imports.

## Context and concrete steps
Core gateway types/port and ResolvedGateway own the additive capability. Raw NMI responses and exact XML parser own the separate transport result; NMI adapter maps it. payment_method_metadata.rs consumes it without changing storage equality or fences. Restore Classic financial descriptor parsing to the tag behavior and preserve JSON expiry. Reuse one exact-query parser and selector binding implementation so metadata cannot bypass envelope/identity validation. Add tests at raw parser, adapter and PostgreSQL workflow boundaries. Add only test dependencies if needed for actual adapter-to-database regression.

Run cargo fmt --all; targeted raw/client/core tests; serialized PostgreSQL metadata/upgrade tests. Then scripts/check-public-api.sh, scripts/jig check fmt, clippy, contract, sqlx, and RUST_TEST_THREADS=1 scripts/jig check test, recording typed work-check receipts. Complete same-scope independent Claude/Codex review rounds using comprehensive-review skill and verified fingerprints. Reviewers remain read-only and compare findings with master before fixes. No publishing or release tag is part of this implementation.

## Validation and acceptance
Old XML-shaped approved evidence with NULL expiry, parked after local application failure, must successfully reconcile from the same XML using the new financial query. The immutable charge evidence must match its original snapshot. The separate metadata query must subsequently fill method expiry, with unchanged financial snapshots and no provider mutation. Cover non-null descriptor conflicts, wrong identity, malformed expiry, duplicate/conflicting fields, query throttling and existing replacement/scrub races. Characterize Classic/XML legacy descriptors and existing JSON expiry, prove unchanged provider implementations still compile, and demonstrate metadata results cannot be used as approval evidence. Fresh required gates and source inspection must establish no schema changes or master feature imports.

## Idempotence and recovery
Work only on this branch; preserve .agent/state JSONL append-only. Tests use disposable databases and loopback synthetic responses, never live financial writes. Run database tests serially. Do not repeat or restart a live reviewer/check handle after a timeout. On a failed check fix the specific defect and rerun affected checks; final full checks follow the settled source.



Implemented separate raw/core metadata observation types and a default gateway capability, with NMI-specific enrichment and shared selector/transport checks. The refresh now uses that capability; financial Classic/XML descriptors match 0.5.2 and JSON expiry is preserved. Core/NMI suites passed (111 core, 24 adapter, 225 raw client). The actual NMI-adapter/PostgreSQL upgrade regression passed: old NULL-expiry evidence reconciles, conflicting retained last-four is rejected, subsequent metadata repair fills expiry, and all financial rows remain unchanged. Independently ran three characterization probes against the actual v0.5.2 tag, confirming Classic alias behavior, XML retained evidence and JSON expiry. All passed; the temporary checkout was removed and its patch/log preserved in /tmp. Added only a test dependency on the existing NMI adapter for the end-to-end regression; no runtime dependency/version/schema changes. Preparing focused comprehensive review round 1.

Focused review round 1 uses isolated checkout /tmp/syrup-expiry-review-x7lx1l55 at cd5970b with complete fingerprint b4e4a872330477a5775cf6f30a31eb6704481512f0673eab122794621b77b893. Codex completed independently with no actionable findings and a matching final fingerprint; Claude is still running. Codex noted the supported baseline is released 0.5.2, not any interim enrichment commits: no remote branch/tag contains those commits, and no deployment or live payment writes were performed in this session. Its XML-to-XML replay scope limitation is retained; cross-format financial response interchangeability is not newly claimed. The 27 metadata PostgreSQL tests passed in 76.79 s, public API docs/doctests passed, fmt/Clippy/contract passed, and Rust 1.88 workspace check plus extracted standalone NMI-client tests passed. Schema immutability, Rust LOC and guide/map checks passed. Broader financial normalization evolution is separately tracked by syrup-rail-x19 for 0.6.0, without blocking the compatible metadata release.


Focused comprehensive review round 1 completed with Claude (opus, restricted file access) and native Codex on working-tree snapshot b4e4a872330477a5775cf6f30a31eb6704481512f0673eab122794621b77b893 at cd5970b. Parent and both reviewers verified a complete unchanged scope. Codex reported no actionable findings.

Merged findings and disposition:
- Low, Claude: clarify that Classic cc_type/cc_exp HTTP-error evidence detection remains intentionally more conservative than v0.5.2. Accepted; README and CHANGELOG now distinguish financial descriptor compatibility from this safety fix. The change is absent from master and retained.
- Low, Claude: clarify the one-way metadata type projection and strengthen the compile-fail example against unrelated import failures. Accepted; rustdoc now describes the exact API boundary and pairs the negative example with a positive public API example.
- High, Claude: JSON financial expiry versus NULL XML query expiry can fail immutable cross-format replay. Verified this asymmetry exists in both released v0.5.2 and master; current JSON parser is unchanged. Not a new 0.5.3 regression relative to the required release baseline. Broader financial normalization/replay evolution is recorded under syrup-rail-x19 for 0.6.0; no financial equality change is made here.
- Low, Claude: equivalent case-only brand duplicates retain a provider-supplied spelling. Not a defect: the existing raw-client README and core/NMI crate invariants require retaining provider evidence and deriving PaymentCardBrand for presentation. Existing brand conformance/portal tests cover this behavior.

Open questions resolved from source: refresh is explicit host orchestration, not an automatic extra call for every approval; complete methods return before gateway resolution. Customer display comes from billing_payment_methods. This fix preserves each v0.5.2 financial parser and does not promise cross-format financial interchangeability or migration from unpublished intermediate commits. No deployment or live provider writes were performed in the session; no remote branch or tag contains the intermediate enrichment commits.

Test gaps addressed: actual NMI adapter/PostgreSQL all-blank repair now supplements fake-provider coverage and verifies all four fields, unchanged financial state, unchanged method identity and entitlement, no extra event and idempotence. It also rejects coherent Declined and Failed NMI metadata observations before a later successful refresh. Existing default-provider tests cover timeout/cooldown/races; both NMI query parser entrypoints share exact selector validation and have explicit invalid-selector/envelope tests. Repeating every database scheduling/race test with a real transport is not needed for this routing change.

Round 1 predates the added all-blank test and documentation corrections. Round 2 will review the updated immutable snapshot before commit.


Focused comprehensive review round 2 completed with Claude (opus, restricted) and native Codex on snapshot 78cd891bf334ce7e2dd118c8949ef21d16d5b64a510c0450acbadaca8d146916 at cd5970b. Both reviewers and the parent verified complete unchanged scope; Codex reported no actionable findings, and Claude reported no correctness, data-loss or concurrency defect in the changed production logic.

Merged findings and disposition:
- Low, Claude: explicitly declare Tokio io-util used by the new test server. Accepted in PostgreSQL dev-dependencies only; runtime dependencies/features remain unchanged.
- Low, Claude: soften the core README's absolute approval-authority wording. Accepted: state the absence of an API conversion and loss of financial response fields, and tell hosts not to reconstruct financial approval from a display observation. The type rustdoc already states the exact boundary.
- Low, Claude: explain brand duplicate tolerance as well as expiry enrichment. Accepted in both NMI READMEs; provider spelling is retained and canonical brand is used for customer display.
- Medium, Claude: change all refresh mocks because the production metadata override is untested. Rejected as unsupported: both actual-NMI/PostgreSQL tests assert enriched expiry while financial query is explicitly asserted to omit it; reverting the production call to query_transaction would fail those assertions. Those tests also cover wrong last4 and Declined/Failed observations. Existing fake-provider tests deliberately cover the default implementation, exercising the same PostgreSQL timeout, cooldown and concurrency code. Replacing all mocks would remove default compatibility coverage without testing the actual NMI implementation.

Remaining gap claims checked: stored VISA versus observed Visa is already covered in card_metadata_refresh_preserves_existing_fields_and_rejects_conflicts. Shared XML parsing rejects non-XML for both entrypoints. The negative doctest detects an added From implementation by unexpectedly compiling; the paired positive example verifies public imports independently. The independently executed v0.5.2 characterization probes and retained fixture pin the financial baseline. Provider-wide cooldown policy is already explicit in the README and source; it follows the existing shared provider policy. The NMI dependency is development-only to test the actual adapter without introducing a provider dependency into PostgreSQL production code.

No new financial behavior, schema change, or master fix is introduced. The final focused pass reviews only the accepted documentation and dev-feature edits on top of the reviewed implementation before the application commit. Full-branch review remains pending.


Focused review round 3 reviewed only the four accepted documentation/dev-feature changes on top of the fully reviewed snapshot 5feb677 in the isolated checkout. Claude (opus, restricted) and native Codex completed; complete unchanged fingerprint c197718a30d2c700e1cc71a29a4135a9f3d2d619138a2e2842f1d6e944e74555 was verified by both and the parent.

No actionable findings after adjudication. Codex found none. Claude confirmed the four edits match source and suggested repeating brand-duplicate details in CHANGELOG. This is an editorial preference: the changelog already identifies the separate metadata query and preserved financial normalization, while both provider READMEs now explain duplicate-brand semantics. No further source change is warranted. Remaining suggestions duplicate the existing layered parser/adapter/portal checks; the positive/negative rustdoc examples and the compiling PostgreSQL targets cover these edits. The test target compiled both before and after the explicit Tokio feature; the latter removes reliance on transitive feature unification rather than repairing an observed build failure.

All three requested focused rounds are complete. Preparing the application commit, followed by a full branch review against v0.5.2 and fresh full required gates. No production logic changed after focused round 2.


Application fix committed as 12c36d7e68930b0b6f7665bf1b879611072b766d, staging exactly the 26 reviewed application/docs files. Full branch review round 1 is running in clean checkout /tmp/syrup-metadata-branch-l9p1dtmb against pinned v0.5.2 9f30107eaf13f4a84fa4a1aaa04299246fedcc09 with complete fingerprint 6a2d03889d75e8ce7bde5653580c252a18515e84aa21717a57ea3fad8a24eb26. Fresh fmt, Clippy, contract, SQLx and serialized full test gates are running on the committed implementation. The mixed preexisting Beads export remains unstaged.

Full branch review round 1 completed for v0.5.2 9f30107 through 12c36d7. Native Codex found no actionable findings. Claude completed with restricted file access and reviewer-attested evidence coverage: 28/28 pages with valid receipts. Page receipts attest access and claimed coverage, not review quality. Both reviewers and parent verified clean complete unchanged scope fingerprint 6a2d03889d75e8ce7bde5653580c252a18515e84aa21717a57ea3fad8a24eb26.

Merged findings and disposition:
- Low, Claude: gateway decorators that forward only the five required methods inherit the metadata fallback and can bypass NMI enrichment. Accepted host-integration documentation fix: the trait rustdoc and core README now explicitly require forwarding the metadata method to retain the wrapped provider capability. No existing production decorator implements PaymentGateway in this repository.
- Low, Claude: SQLx source-chain diagnostics are not covered by top-level redaction assertions. Accepted contract clarification in PostgreSQL README: ordinary Display/Debug omit details; SQLx sources remain available for explicit diagnostics/SQLSTATE inspection and must not be automatically chain-formatted into logs. Removing the public SQLx cause would change the error contract and is not part of this fix.
- Medium, Claude: query 429 extends a provider-wide financial cooldown. Intentional existing policy, already documented/tested. Rechecked official NMI rate-limiting documentation (https://docs.nmi.com/reference/rate-limiting): HTTP 429 spans Payment and Query APIs / shared IP connections and callers must reduce concurrency and wait. Ignoring this signal or waiting for multiple 429s would knowingly continue financial submissions under observed throttling. Hosts authenticate/authorize callers and cap refresh scheduling below payments; the library does not supply a second host security/admission boundary. Peak provider-query load rehearsal is already tracked by syrup-rail-bn4; include metadata backfill volume there rather than introduce a new quota schema/admission policy in 0.5.3.
- Medium, Claude: account FOR SHARE can contend with financial cooldown/configuration writers. This is an explicit operational limitation, already documented with SQLSTATE retry guidance. The lock is acquired in the final revalidation statement after advisory locks, followed only by display UPDATE and COMMIT. It protects configuration/provider identity through the write; dropping it or adding an unlocked final predicate would reopen that race. No provider I/O or host callback runs while held. A no-contention guarantee under arbitrary concurrent load cannot be achieved by removing the required identity fence; bounded host scheduling remains required.
- Low, Claude: unknown brand text is preserved. Already explicit in PostgreSQL README and tests; fill-only repair intentionally preserves present provider evidence. A later approval may replace display evidence (including on the same vault reference), so the claim that only a full method replacement can remedy it is too strong. No change.
- Low, Claude: broaden HTTP-error evidence further to the request spelling ccexp. The patch adds the documented response fields cc_type/cc_exp; it already records the more conservative classification in CHANGELOG/README. No captured provider response or protocol evidence establishes ccexp as a response alias. This is not a missing dependent change, and the released/master behavior for ccexp is unchanged.

Open questions researched: subscription method-pointer writers in enrollment/recovery/replacement share the payment-method advisory domain before writes; other-plan EXISTS is stabilized by that domain. The initial read intentionally uses the pool without a write transaction; the 10-second documented timeout is for provider I/O, not an end-to-end database deadline. Existing index/plan checks constrain the lookup; arbitrary host database/DDL stalls are not claimed to have an overall deadline. Host authentication and caller rate limits are explicit in the threat model and README. g75 remains open only until final validation closes it.

Residual limits retained: no live/staging account response verification, no COMMIT-ack cancellation injection, and no performance characterization of extremely large histories concentrated on a single shared method. Existing tests cover cancellation during active SQL/uncommitted writes, per-subscriber query plans, default-provider behavior and actual NMI-to-PostgreSQL all-blank/upgrade paths. These do not establish host rollout completion.

Only host guidance/rustdoc changes follow this review; no runtime behavior, schema, or financial contract change. Public API documentation/doctest validation passed on the clarification. The full runtime suite is still running on unchanged production logic. A second full branch pass will include the guidance commit.


Full checks completed successfully: 111 core, 24 NMI adapter, 225 NMI client, 269 PostgreSQL and 4 SQLx-gate tests (633 total), plus six doctests; PostgreSQL finished in 601.40 seconds. SQLx, fmt, Clippy and contract passed. The work-check wrapper detected the documentation-only follow-up during its run and therefore did not issue a fresh worktree fingerprint. Public API docs/doctests separately passed on that guidance. Re-running required gates on settled HEAD 0206cb32389e36fdfeaf56d3e2d4f498d29e529f so work finish has fresh receipts. Full branch review round 2 uses the same clean isolated checkout at that HEAD, pinned v0.5.2 base, complete fingerprint 16e466d1daa5800a1ac71a0fccfe8a38f8e1670e34f716ff96beb7f6602046e4. Application source will remain frozen unless a new actionable finding requires a fix.

Full branch review round 2 completed for v0.5.2 9f30107 through 0206cb3. Native Codex found no actionable findings and no breaking change requiring 0.6.0. Claude completed with restricted file access and reviewer-attested evidence coverage: 28/28 pages with valid receipts. Parent and both reviewers verified clean, complete unchanged fingerprint 16e466d1daa5800a1ac71a0fccfe8a38f8e1670e34f716ff96beb7f6602046e4. Receipts establish access/claimed coverage, not substantive review quality.

No additional runtime fix is warranted after adjudication. Claude repeated the documented provider-wide 429 coupling and requested a new mechanical admission/budget mechanism; that is a separate policy feature, not a correction to the existing system-wide throttle semantics. Host capacity rehearsal already has syrup-rail-bn4 and will include metadata backfills. A new required budget/permit or persisted quota model must not be introduced in this compatibility patch. Claude also proposed making the metadata method required or adding capability provenance to address decorators. The required-method option is breaking and will be recorded as a 0.6.0 design follow-up; the source-compatible 0.5.3 default plus explicit forwarding guidance remain.

Claude noted historical plan statements that manifests/lockfile and NMI dependency edges were unchanged. Those were earlier snapshots, before 12c36d7; this current completion record supersedes them for final release scope: PostgreSQL now has a development-only NMI dependency and explicit Tokio io-util test feature, with one internal dev-dependency edge in Cargo.lock. No version, runtime dependency, third-party dependency version or schema artifact changed. These test-only additions enable the actual NMI-adapter/PostgreSQL regression. The PostgreSQL guide's provider-neutral production ownership remains intact; no policy exception is needed to write a provider integration test.

The conservative HTTP-error classification is already explicit in CHANGELOG and NMI README. Further review-queue sizing prose adds no missing contract. Relevant positive evidence tests plus the existing HTTP-error regression matrix passed; no unrelated retry classification was changed.

Open questions resolved/qualified: resolved_at is database clock_timestamp set inside the serialized approval writer, not a host timestamp; schema does not promise uniqueness. Equal timestamps use the explicitly deterministic (resolved_at, id) order, without claiming UUID creation order or a new durable commit-sequence contract. The method/subscription timestamp fences intentionally allow conservative ChangedDuringQuery on unrelated writes; bounded host retry scheduling is documented. Equivalent brand duplicates use the existing order-independent collector that selects a provider-sent spelling (lexicographic min when no exact normalized spelling is present); both orderings are tested. Scope/subscriber authorization is mandatory before invoking the API, so Ineligible is not an unauthenticated probing interface. Synthetic fixtures and code review do not replace host staging validation.

Three focused review rounds and two complete full-branch review rounds are finished. No additional code change follows this pass. Fresh required gates are still running on unchanged HEAD 0206cb3 before the plan is closed and tracker follow-ups are recorded.


Final gate audit passed with fresh receipts on settled 0206cb3: contract, tests and SQLx all passed/fresh; fmt and Clippy passed too. Full suite: 633 tests plus six doctests, including 269 PostgreSQL tests in 623.51 seconds. Final Rust 1.88 workspace all-target check passed in 15.51 seconds. Implementation and all required review loops are complete; closing the workflow, then updating Beads and committing this completion record. Host staging and release publication are separate acceptance steps.
