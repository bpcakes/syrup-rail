Implement the accepted audit items sequentially: make report evidence/capacity commit only after successful parsing (R-18), type external-reversal resolution tuples with compatibility and live-row validation (R-12), then replace payment-attempt lookup lock booleans with explicit find/lock operations (R-22). Verify each acceptance matrix and finish with the relevant repository gates.