Keep real SQLx/Postgres transaction semantics. Record each begin subject with transaction-local pending BillingEvent payloads; publish to the shared committed sink only after SQL commit succeeds. Add direct commit, rollback, drop, and commit-failure characterization, including exact subject/payload and local event order. Preserve semantic-only assertions for concurrent transactions.