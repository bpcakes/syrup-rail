Research the exact-query and gateway identity contracts; centralize payment-method replacement approved parking so charge observation and durable fallback cannot drift; enforce identity-diagnostic quarantine in GatewayPaymentOutcome; clarify and test order-only exact-query semantics; run all required gates and repeat comprehensive review until no critical or medium findings remain. Log low findings in Beads rather than fixing them.