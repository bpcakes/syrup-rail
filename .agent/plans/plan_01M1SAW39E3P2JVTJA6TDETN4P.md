Resolve approval-evidence and replay invariants structurally, preserve immutable v5 artifacts, research cutover and release contracts, validate changes, and repeat independent Opus/Codex/Cursor branch reviews. This plan remains open.

Current review checkpoint: 978f583468e124d00c2032211dcceda3265e5453, base 9f30107eaf13f4a84fa4a1aaa04299246fedcc09. The clean branch fingerprint 46dd7866664aaf9bf3ad38edd2b6b503ed1ed40c76537aee6c121eb83353a8c8 matched before and after every reviewer. Native Codex completed; Opus completed with 207/207 valid page receipts; Cursor completed with limited 206/207 receipts (page-0038 missing). Receipts attest access, not review quality.

Open release blocker (Claude, Cursor; also confirmed by the preceding native Codex review): nonempty v5 declined host attempts and unsubmitted failures without charges change from safe reserve/release to unsafe after v6. An isolated actual upgrade reproduced all four transitions. The preflight and read-only audit now inventory these terminal populations and the guide explicitly blocks affected cutovers. Their financial retry eligibility is unchanged by this follow-up. The user was asked whether to require authoritative recovery evidence or preserve v5 terminal retry eligibility; no answer has arrived. Do not infer absence from old provider strings, an empty query, or a local note, and do not invent a retry override.

Structural observation fix (Codex): cumulative attempt approval risk was combined with current identity and then reused as a charge observation. Regressions reproduced both a manufactured charge from a later decline and an immutable-charge replay error that rolled that decline back. Charge recording now receives the original observation separately from accumulated attempt evidence. Payment-method reconciliation keeps quarantine diagnostics through this boundary instead of recycling accumulated evidence as a fresh outcome. Existing quarantine coverage caught and prevented identity restoration; all 75 enrollment-application tests subsequently passed, as did all 22 host-application tests and both new host observation regressions.

CI correction: the 978f583 Rust job failed manual_failure_is_policy_safe_atomic_eventful_and_host_exact with canonical operator review state invalid. A deterministic UUID reproduced the same failure: the fixture's hyphenated UUID order did not satisfy the generated-order grammar and sometimes matched the raw-card heuristic. Use the validated generated-order constructor, retain the triggering UUID, and normalize sibling generated-order fixtures to the simple UUID suffix. Production validation remains strict. The regression passes after correction.

Release branch research: GitHub's default remains master, but the release environment permits main and commit 7ddb8ea explicitly prepared 0.6.0 on main. Merge 2b8b51b overwrote that release-specific guard; 978f583 restored it. Keep the main-only publishing guard and master Jig default, document the distinction, and run push CI on both main and master (Codex's supported trigger finding). No publication or environment permission change is authorized or performed.

Other review adjudications: unclassified review-required holds are the documented fail-closed contract, and no provider-negative proof can be inferred from absent query results. Three renewal mode queries are explicitly budgeted in the README; NMI publishes no universal numeric limit, so an invented concurrency ceiling would mislead hosts. The v6 maintenance cutover stops all old writers; the lock comment and guide now clarify that preserving the pre-v6 approval-writer key does not repair pre-0.6 scrub workers' different key. Structured approval risk intentionally survives malformed sibling fields without conferring approval authority. Exact-query failure no longer fabricates a provider condition, preserving observed evidence; hosts route on typed attempt state. Invalid sale boundaries retain request-bounds/transport coverage. Immutable v5 bytes remain governed by the crate guide.

The previous payment-method reconstruction fix routes direct and shared service entry points through one validated reservation; its operation-specific error regression remains covered. Earlier 12c3008 CI passed. The 978f583 CI failed the deterministic fixture case. The follow-up now passes the complete workspace test batch (including 307 PostgreSQL tests), contract, SQLx, formatting, Clippy, and Rust LOC gates. Batch receipt receipt_01M1SSP68SAGABYDTV7R1HPWS8 records all three required gates as fresh and passed. Final review, pushed-head CI, and migration policy remain pending.
