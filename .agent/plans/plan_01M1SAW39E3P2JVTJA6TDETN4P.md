Resolve approval-evidence construction and replay equality structurally; add schema-v6 preflight/audit guidance and tests; preserve immutable schema-v5 artifacts while documenting their header erratum; correct schema assertion docs; encode readiness states without impossible variants; preserve approval evidence monotonically; restore focused regression coverage; run full gates and repeat Opus comprehensive review until clean.

Review checkpoint: 12c3008707b037cfdb9aa9c4af6ac37f9dd9d59d is pushed and all GitHub Actions checks pass. The independent branch review fingerprint matched before and after all reviewers. Cursor reported no actionable findings with 206/206 page receipts. Opus completed with limited 104/206 receipts; native Codex confirmed a terminal-history migration regression.

The migration finding remains open: nonempty v5 declined host attempts and unsubmitted failures without charges change from safe reserve/release to unsafe after v6. An isolated integration reproduction confirms both transitions. The read-only inventory can be extended to include these terminal populations; the implementation is prepared outside the branch while the compatibility policy is decided. The user was asked whether to require authoritative recovery evidence or preserve v5 terminal retry eligibility. Do not infer absence from old provider strings or silently invent a retry override.

The separate Opus reconciliation finding is fixed by routing direct and service payment-method reconciliation through the same attempt/provider reconstruction, then handing a validated replacement reservation to its existing locked application owner. This removes the redundant transaction and keeps operation-specific error messages.

Other review adjudications: immutable v5 artifact bytes remain governed by the repository guide. Discount admission asks only whether any current subscription exists while holding the aggregate lock; ranking stabilization is not part of its boolean contract. Renewal readiness traffic and lifecycle retention costs are already documented deliberate tradeoffs. Explicit approved outcomes continue to have their own authoritative evidence path; deprecated raw helpers remain compatibility exports. Release workflow and documentation both select master; GitHub currently reports that branch itself as unprotected, so this review does not claim branch protection is configured.

Validation of the reconciliation fix: all 10 existing reconciled-outcome tests and the new direct/service gateway-identity regression passed. Formatting, Clippy, contract, SQLx, and Rust LOC checks passed. The full local batch passed 303/304 PostgreSQL tests; schema_v1_upgrade_preflight_reports_only_blockers failed at Docker ContainerStart/PortNotExposed before its assertions and passed on an isolated rerun. Do not report that full local run as green. The plan remains open for the migration policy and final review loop.

Release configuration research: GitHub release environment uses a custom deployment branch policy that currently permits main, while the workflow requires master. This blocks manual publication, not PR CI. The PR description records the mismatch; no release permissions were changed.

Release configuration resolution: commit 7ddb8ea explicitly selected main for the 0.6.0 release; merge 2b8b51b overwrote that line-specific policy with master. Restore the main-only workflow guard and release instructions, preserving the existing release environment permission rule. The repository default remains master, but it does not supersede the explicit 0.6.0 release decision. No deployment permissions or publishing actions are changed.
