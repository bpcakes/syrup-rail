Full-branch round 3 completed at 1af44d1 with independent Claude and Codex reports and verified complete fingerprint 2c3e32081e824f6f0b81064472bb8e19e7e73d9160ad839bf8eacd3159ca2117. Record the reproduced parser-upgrade replay blocker in Beads; do not change financial replay contracts or import fixes from master. Fix only the cancellation regression test race, which has no master counterpart. Preserve rollback, unchanged financial/method snapshots, and successful later refresh assertions. Validate the timeout interleaving, run formatting and full required checks, and commit the test correction. Stop at the third full-branch round; retain the release blocker explicitly.

The upgrade probe reproduced rejection of a parked v0.5.2-shaped approval after expiry enrichment, and the retained-evidence control succeeded (one test, 6.76 s). Logged syrup-rail-g75 and made syrup-rail-20u depend on it; release remains blocked. The original cancellation assertion failed under a 350 ms delayed abort. The corrected test passed both normally (2.57 s) and with the same delayed abort (3.16 s), while preserving full financial/method snapshots, single-connection reuse, and subsequent refresh success. The temporary probes are isolated from the release checkout. A focused Claude/Codex review of the final test-only diff is running before commit. Full-branch review is complete at the agreed third round; no additional full-branch pass is planned.

Focused precommit review round 1: Claude and Codex independently found that the timeout-tolerant assertion could omit the unique cancellation coverage. Its matching complete fingerprint was 0f4ee44bedeb7cf0fac6b7a9d7b6931328287e7ec47b8e2392eea8301ce05a6d. Replaced that approach with a cfg(test)-only task-local pause after fill_missing_fields and before commit. The test waits for this explicit signal, verifies the task is still pending and the write is uncommitted, then requires actual task cancellation. There is no active SQL statement or lock-wait timer in this pause, and production builds exclude it entirely. Existing lock-timeout tests remain separate. The superseded full-test run was cancelled and will be rerun on the corrected snapshot. The financial upgrade blocker remains open; no replay-policy changes were made.

The cancelled validation invocation accidentally used the default parallel test runner. Its PostgreSQL containers exhausted startup capacity; cleaned only the 74 harness-labelled containers created during this run whose project names match this repository tests. All older containers were excluded. Subsequent full checks explicitly set RUST_TEST_THREADS=1, matching the prior passing validation. The first synchronized-test run failed at container startup, before test execution, and is not evidence against the code. Native Codex completed focused review round 2 with no findings; Claude is still running.

Focused round 2: Codex found no issues; Claude correctly distinguished active-statement cancellation from idle transaction cancellation. Both fingerprints matched 255e050ab8f52c92dd4ef332115334e16db99230f0af21d71b611a3cd029e2ae. Keep both tests. The original row-lock cancellation scenario now scopes a cfg(test)-only timeout override to its own transaction; normal runtime deadlines are unchanged. It still observes the actual lock wait, requires cancellation, and verifies rollback/connection reuse. The after-write pause now requires Updated, so its notification proves a real uncommitted write. Both focused tests pass (2 tests, 4.94 s). Focused round 3 reviews the resulting three-file test-only change under fingerprint 7e534d70084f312f0ef84fc06ad590dd697342ee8a2e7aa1643073e122636285. The full serialized suite will run only after that review settles.

Focused precommit round 3 is complete (last requested round). Claude and Codex both reviewed fingerprint 7e534d70084f312f0ef84fc06ad590dd697342ee8a2e7aa1643073e122636285; the parent independently confirmed it unchanged and complete after both reports. Codex found no actionable defects. Claude identified a medium test-only unbounded server timeout: corrected by extending both deadlines to a finite 60 s only in the scoped test transaction. Also restored the distinct application name and made unexpected completion report the actual result instead of only a notification timeout. Extended the existing normal-timeout regression to contend the gateway-account row lock, as requested by the coverage finding. These final review responses were locally inspected; no fourth external review was run.

Open questions were resolved from pinned local dependencies. postgres-test-harness 473d280 uses a semaphore for its connection budget, not a hard per-database connection cap (server max_connections is 300); DatabaseLease::drop queues cleanup and ContainerOwner::drop shuts down its owned container. This contradicts the claim that every early test return necessarily leaks the lease/container, so no general test teardown rewrite was added. SQLx 0.8 Transaction::drop explicitly queues rollback for the next asynchronous connection operation, including pool return. The tests intentionally verify this public transaction behavior. The task-local hooks remain cfg(test)-only permanent regression instrumentation.

Residual review limitations: cancellation after COMMIT is sent but before acknowledgement is not fault-injected; current tests cover in-flight SQL and uncommitted writes. Existing field-level metadata tests cover actual repaired values; successful subsequent refresh is also the behavioral lock-release assertion. No separate pg_locks assertion is necessary. Full-branch review still has the reproduced Codex compatibility blocker syrup-rail-g75, and the Claude account-lock contention observation remains an operational consideration: the share lock is required to preserve configuration fencing. No master implementation was copied or breaking redesign made. The final correction is test-only and does not clear the release blocker.


Final verification passed on the final four-code-file correction. Focused metadata suite: 27 passed in 78.38 s. RUST_TEST_THREADS=1 scripts/jig work check ran fmt, Clippy, contract, SQLx and the full all-features workspace suite successfully; batch receipt receipt_01M1YS4N058HC88WR3P59Y26FT, test receipt receipt_01M1YS4MYRW2D3AMNQ7S6WWXBB. Counts: 625 unit tests (110 core, 24 NMI adapter, 224 NMI client, 267 PostgreSQL), 4 example tests and 4 doctests; no failures or ignored tests. PostgreSQL tests completed in 727.24 s. Schema artifacts, Cargo manifests and lockfile remain unchanged from v0.5.2. The owned review checkout was removed after verified fingerprints and archival of its reviewed diff. This completes the review/test-correction checkpoint, not release readiness: syrup-rail-g75 remains open and blocks syrup-rail-20u.