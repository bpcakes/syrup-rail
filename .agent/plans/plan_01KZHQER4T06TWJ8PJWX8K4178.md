Implement the fresh Fowler audit in order: centralize the approved transaction/event finalizer, move lock-free approved-evidence persistence into processor_charges, and consolidate subscriber mutation admission/readiness policy. Preserve public APIs, persistent evidence semantics, transaction/lock order, provider-I/O ordering, and operation-specific renewal/host behavior. Commit each independently green slice and finish with full repository gates.