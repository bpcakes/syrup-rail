Implement the three requested simplification beads sequentially: model discount-claim lifecycle as a data-bearing enum (R-01), encode decoded decision-field state as a closed enum (R-17), then carry a closed host-charge resolution command into persistence (R-27). Preserve public and persistence compatibility, validate each acceptance matrix, and finish with current-worktree repository gates.