Wire standalone package validation and development package preflight into regular CI; cover all build inputs; resolve event-specific file-size comparison bases and add regression coverage. Preserve publishing and shipped-schema policy. Verify locally and confirm remote CI on master.