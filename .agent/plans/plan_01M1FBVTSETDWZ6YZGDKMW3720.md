Restore the aggregate identity trust invariant across Classic, JSON, and XML foreground parsing. Preserve a valid identifier only when its peer is missing; if either identity is invalid, conflicting, or rejected at the NMI adapter boundary, clear both identifiers while retaining field-specific diagnostics and determinate non-approved payment status. Update documentation and add regressions proving contradictory responses cannot become durable sibling evidence.