Merge release/0.5.3 into master, preserve unreleased 0.6.0 approval evidence and schema v5, adapt metadata repair to refactored workflows, reconcile release documentation and tracker records, and validate contract, formatting, complexity, public API, clippy, SQLx and backend tests.

Integration combines master 6fccde0 (including the local unreleased schema-v5 consolidation) with release/0.5.3 at 28861f3; v0.5.3 tags 57a705a. Retained all 0.6.0 version requirements, master schema artifacts and typed approval-evidence behavior. Imported metadata-only provider queries, bounded saved-card repair, all release regression modules, publishing dependency order, release notes, and independent Beads changes. Adapted repair to the shared v5 approval/scrub lock and typed account accessors; adapted imported approval fixtures and renewal helpers. Removed the patch-only test-layout exception. Both parent branches remain represented in append-only plan/receipt/session history. Contract, formatting, Rust file-size policy, immutable-schema checks, Rust 1.88 Clippy, SQLx and public API documentation/doctests pass. Full serial workspace tests are running before the merge is finalized.

Final validation passed: serial all-feature workspace tests (jig.test receipt_01M203EDT0KP8GDTFMBYKEWEWR, 1234.4 seconds), contract and SQLx (batch receipt_01M202GK5PAXH2NPAFG4SPQ5KG), Rust 1.88 Clippy (receipt_01M202CABYHYX3DMFCV00YYV49), formatting (receipt_01M202A2G7BGRR4H4XAS4FSDD0), Rust file-size policy, shipped-schema immutability and scripts/check-public-api.sh including documentation tests. Required gate evidence is fresh for the final code. Review confirms all source conflicts resolved, no schema artifact edits, unchanged 0.6.0 version requirements, preserved 0.5.3 metadata regressions with typed-evidence/shared-lock adaptations, and both parent histories retained. Completing a local merge into master; no push or publication requested.

Correction to the preceding note: the test command passed, but work finish rejected its batch freshness because the Git merge index was resolved during the run (before fingerprint 1d3b08a7990ea1bd741b9ff55423b840b50af68a; after 18187a53ba82bbaff058c68c1e735daf6d8b4e7c). No source edits followed test start. Contract and SQLx receipts are fresh. Repeating the required test gate on the settled merge state so work finish can validate it without changing or overriding recorded evidence.

To avoid repeating per-test container startup overhead, interrupted the receipt-only rerun and provisioned a dedicated disposable PostgreSQL 18 container on loopback. The harness explicitly supports POSTGRES_TEST_ADMIN_URL and still creates isolated databases per test. Running the same complete serial all-feature test command through the required Jig gate against that server; no tests are filtered or changed. The first complete run already passed using default Testcontainers.

The complete unchanged serial all-feature workspace suite passed again using the supported external-server harness mode: receipt_01M203Z8JP99XY7Z72RC2GRZ48, batch receipt_01M203Z9MXMKSSANGQB627GFVJ, 295.3 seconds. Every required gate is now passed and fresh (contract, tests, SQLx) at fingerprint 18187a53ba82bbaff058c68c1e735daf6d8b4e7c. The temporary server is disposable and will be removed. This confirms the initial default-Testcontainers pass without altering tests or overriding receipts.