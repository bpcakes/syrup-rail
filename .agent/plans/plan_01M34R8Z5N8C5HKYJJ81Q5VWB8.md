Production IdentityPro 0.5.3 renewal investigation found five submitted attempts with no provider transaction; sandbox v5 rejects vault recurring fields while Classic succeeds. Implement compatible Classic renewal transport and minimal owner-controlled recovery API if required, preserving attempt history and at-most-one submitted replacement. No schema rewrite or v0.6 adoption. Consumer IdentityPro deployment; plan removed/archived after release verification. Validate exact request fields, uncertainty handling and database concurrency; package compatible 0.5 patch for IdentityPro.